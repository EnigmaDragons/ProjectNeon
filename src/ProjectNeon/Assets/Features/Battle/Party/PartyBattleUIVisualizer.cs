using UnityEngine;

public sealed class PartyBattleUIVisualizer : MonoBehaviour
{
    [SerializeField] private HeroBattleUIPresenter heroPresenter;
    
    // @todo #1:15min Dynamically Create all Hero Battle UI during Battle Setup 
}
